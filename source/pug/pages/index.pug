extends ../components/base/layout

block variables
  - const title = "Погнали" //- на страницах кроме главной используйте О нас — ProjectName
  - const wrapperMods = ""
  - const hasHeader = true
  - const headerMods = ""
  - const hasFooter = true
  - const footerMods = ""
  - const currentUrl = "index.html" //- для остальных страниц пишем пустую строку или индекс активного headerLinks

block main
  main(data-index)
    h1.visually-hidden Главная страница проекта "Погнали"
    +hero()
    +about()
    +directions()
    +companions()
    +profile()
    +intro()

block modals
  +modal("modal--feedback")(data-modal="feedback")
    +modal-feedback()
